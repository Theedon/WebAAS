name: Formatter
on:
  push:
    branches:
      - "main"
jobs:
  build-and-format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install all packages
        run: npm ci
      - name: Run Prettier
        run: npm run format:write
